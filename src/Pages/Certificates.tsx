import * as React from "react";
import Box from "@mui/material/Box";

const Certificates = () => {
    return (<Box sx={{ flexGrow: 1, px: 3 }}>
certificates
    </Box>)
}

export default Certificates;